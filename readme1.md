# 1일차(02/26)
## 공부법
    

## 1. 절차지향 VS 객체지향

| 구분 | 절차지향 (Procedural) | 객체지향 (Object-Oriented) |
|---|---|---|
| **대표 언어** | C | Java, C++, Python |
| **실행 속도** | 빠름 | 상대적으로 느림 |
| **중심 개념** | 함수(Function) | 클래스(Class) |
| **처리 방식** | 순차적 처리 | 객체 단위 처리 |
| **특징** | 절차 중심 | 역할 분담에 유리 |

## 2. 구조체와 클래스

#### 구조체
- `필드` : 구조체 안의 요소
- 필드에 들어갈 수 있는것은 `변수` 뿐
    > 멤버 변수
- C++에서는 구조체에 멤버 변수와 함수를 집어 넣을 수 있다
    > Java는 불가능
- 구조체를 사용하기 위해선 구조체 변수를 선언

#### 클래스
- C언어의 `구조체에서 발전`된 형태
- 필드 안에 함수를 집어 넣을 수 없을까? >> `클래스`
- 클래스를 사용하기 위해선 클래스에 맞는 `객체`(변수)를 선언


## 3. 객체지향
> Java = 완벽한 객체지향언어

> namespace - 객체지향에서의 작업공간, 변수이름이 같아도 충돌XX

> 팀플을 하기에 안성맞춤인 언어

### 제어문
#### 조건문(선택 제어문)
> if

> switch-case

#### 반복문(반복 제어문)
> for
    
> while

- 서로 병행해서 사용이 가능

### #include<stdio.h>
> `#` - 전처리기가 담당하라는 뜻, ex) #include, #define 등등

> `.h` - 헤더파일을 나타내는 확장자

> main안에서 작성하는 것들도 이 헤더파일에서 불러오는 것

지금은 main에서 return 값을 돌려주지 않아도 에러가 뜨지 않음

#### return -1; 을 해도될까?
> 된다

> 정수의 값이면 다 된다

#### 그럼 왜 return 0을 하는걸까?

> 정상적으로 실행되고 마쳤다라는 의미를 담았음

> 일종의 개발자들의 약속

### 살펴보기

> int a ;

- 메모리공간에 int크기의 a라는 변수를 할당
- 메모리에 접근하기 위해서는 주소가 필요
- 주소대신 a라는 변수 이름으로 접근을 하는 것

> a = 10;

- a라는 메모리 공간에 10을 집어넣음

> int* pa;
- 포인터 변수 선언, 기능이 없고 나는 포인터 변수다 하는 기호일 뿐

> pa = &a;

> *pa = value;    // 여기에 *은 연산자

#### 포인터 연산자

> `*` : 간접 참조 연산자

> `&` : 주소 연산자


#### 멤버 접근 연산자

> `->` : 화살표 연산자

> `.` : 점(도트) 연산자

a & b : 비트(논리) AND
a && b : 논리 AND

컴퓨터는 거짓을 판단함
0이 아니면 다 참 
거짓 하나만 판단한다

void print_list(struct address*);
- 변수명이 없어도 에러가 안뜬다
- 선언이기 때문 컴파일에게 알려주는것 뿐이기에
- 저장할 수 있는 공간을 따로 만드는것은 아니기 땜에

void print_list(struct address* lp){
}
배열이름은 첫번째 요소의 주소
그러므로 포인터로 받아야 한다

배열은 제어문(for문)을 사용
> 배열이 어렵지 않음

포인터배열 : 배열인데 내용물이 포인터 인것

배열포인터 : 포인터인데 얘가 통째로 배열을 가리키고 있는 것

이중포인터 : 포인터의 포인터

### 메모리 구조와 변수의 저장 위치
컴퓨터가 프로그램을 실행할 때 메모리는 크게 다음과 같이 나뉜다

#### 영역별 특징
> **데이터 세그먼트** : 전역 변수, static변수가 저장
- 문자 리터럴(e.g., `"hong"`) 자체가 저장되는 공간(read-only)

> **스택** : 지역 변수, 매개 변수가 저장됨
- 함수가 종료되면 자동으로 소멸, `const` 지역변수도 이 영역에 생성

> **힙** : 동적 할당(`malloc`, `new`)시 사용되는 영역

> **코드 세그먼트** : 작성한 코드(기계어)와 함수가 저장되는 곳
- 함수 이름 자체가 이 영역의 주소

#### 문자열 선언의 차이
> char arr[] = "hong";   // "hong"을 스택 영역에 복사 (수정 가능)

> char* arr1 = "hong";   // arr1은 스택에, 실제 "hong"은 데이터 세그먼트에 존재 (수정 불가)

# 2일차 - chapter 01. C 언어 기반의 C++
## 01-1. printf와 scanf를 대신하는 입출력 방식

### 1. C++과 C의 관계
- C++은 약 20%정도 C의 문법을 그대로 사용
- C 문법 위에 객체지향 개념이 추가된 언어

### 2. 네임스페이스(Namespace)

#### 네임스페이스란?
- 이름 충돌을 방지하기 위한 개념
- 서로 다른 공간에 동일한 이름을 허용할 수 있게 해줌

#### `std` 네임스페이스
- std는 C++에서 기본적으로 제공하는 표준 네임스페이스
- 표준 입출력, 문자열, 컨테이너 등 기본 기능들이 포함

### 범위 연산자 `::`
- `::`-> 범위 연산자
- 특정 네임스페이스에 속한 이름을 지정할 때 사용

> std :: cout
- std 네임스페이스에 있는 cout 객체를 사용하겠다는 의미

### 3. 출력(Output)
#### `cout`
- 표준 출력을 담당하는 객체
- 콘솔에 데이터를 출력하는 역할
- 출력에 특화된 객체
> std::cout << "Hello";

#### 출력 연산자 `<<`
- `<<` -> 출력 연산자
- 왼쪽 객체에 오른쪽 데이터를 출력하라는 의미

#### `endl`
- `endl`도 객체
- 출력이 끝났음을 알림
- 줄바꿈 + 출력 버퍼 비우기(flush) 역할
> std::cout << "Hello" << std::endl;

### 4. 입력(Intput)
#### `cin`
- 표준 입력을 담당하는 객체
- 키보드 입력을 받아 변수에 저장
> std::cin >> value;

- 의미 : 
    -> cin으로 받은 값을 오른쪽 변수에 저장하라

#### 입력 연산자 `>>`
- `>>` -> 입력 연산자
- 데이터를 변수로 전달하는 역할

### 5. 변수 선언과 객체
#### C++의 변수 선언 특징
- 변수 선언 위치에 제한이 없다
- 필요할 때 자유롭게 선언 가능
- 사실상 C도 동일하지만 위쪽에 선언 추천

#### 객체와 타입
- 객체 선언시 **타입** 필수
- 객체지향에서의 타입 = **클래스**

#### 인스턴스
- 특정 클래스로부터 생성된 객체를 **인스턴스(instance)**라고 함
- C에서 `int a;` 처럼 타입이 오는 개념과 동일한 구조

### 6. `cin`과 `cout`의 클래스 관계
- `cin`
    - `ostream` 클래스로부터 생성된 인스턴스
- `cin`
    - `ostream` 클래스로부터 생성된 인스턴스

### 7. 입력 버퍼(Input Buffer)
#### 입력 버퍼란?
- 키보드로 입력한 데이터는 즉시 변수로 들어가지 않는다
- 먼저 `입력 버퍼`에 저장된다
- 입력 함수는 이 입력 버퍼에 저장된 데이터를 읽어 변수에 저장한다

#### 입력 과정의 흐름
1. 사용자가 키보드로 입력
2. 엔터를 누름
3. 엔터 전까지의 모든 내용이 **입력 버퍼에 저장**
4. 입력함수(`cin`, `scanf`)가 호출되면
 -> 입력 버퍼의 데이터를 읽어 변수에 저장

#### 문자열 입력과 `cin`
- `cin`은 입력 버퍼에 저장된 데이터를 읽는 개체
- 공백을 기준으로 데이터를 읽음
- `cin`이 호출되면
    -> 버퍼에 있는 문자열을 변수로 가져옴
- scanf도 마찬가지

## 함수 오버로딩

### 1. C와 C++의 함수 구분 방식
#### C 언어
- **함수 이름**만으로 함수가 구분됨
- 매개변수가 달라도
    -> **함수 이름**이 같으면 **같은 함수**로 취급


#### C++ 언어
- 함수이름 + 매개변수 목록으로 함수를 구분
- 매개변수의 개수, 타입, 순서가 모두 같아야 함
- 한환형(출력 타입은) 함수 구분에 포함 XX

| 언어 | 함수 구분 방식 | 오버로딩 |
|:------|:---------------|:--------:|
| **C** | 이름 | X |
| **C++** | 이름 + 매개변수 | O |

### 2. 함수 오버로딩
#### 함수 오버로딩이란?
- **같은 이름**의 함수를 여러개 정의 하는 것
- 단, 매개변수 선언이 서로 달라야 함
- 컴파일러가 호출 시 전달된 인자를 기준으로 어떤 함수를 호출할지 결정
```
int sum(int a, int b);
int sum(int a, int b, int c);
double sum(double a, double b);
```
> 위 함수들은 서로 다른 함수로 인식됨

### 3. 디폴트 매개변수(Default Prameter)
#### 디폴트 값이란?
- 함수의 매개변수에 기본값을 미리 설정하는 것
- 함수 호출 시 해당 인자를 전달하지 않으면
    -> 설정된 디폴트 값이 자동으로 사용
```
void print(int x = 10);

print();    // x = 10
print(5);   // x = 5
```

#### 부분 디폴트 값(Partial Default Parameter)
- 모든 매개변수에 디폴트 값을 줄 필요는 없음
- 일부 매개변수만 디폴트 값 설정 가능
- 이를 **부분 디폴트 값 설정**이라고 함
```
void func(int a, int b = 10);

func(3);        // a = 3, b = 10
func(3, 5);     // a = 3, b = 5
```

#### 디폴트 값 설정 규칙**(중요)**
- 디폴트 값은 반드시 **뒤쪽 매개변수**부터 설정해야 한다
> void func(int a, int b = 10, int c = 20);  // ⭕ 가능

> void func(int a = 10, int b);  // ❌ 불가능

#### 이유
- 함수 호출시 인자는 **왼쪽부터** 순서대로 매칭
- 앞 매개변수가 디폴트면 
-> 뒤 매개변수에 어떤 값을 넣어야 할지 판단 불가

### 4. 인라인 함수
#### 인라인 함수란?
- 함수 호출시 분기(branch)없이 함수정의가 **호출** 위치에 그대로 삽입되는 함수
- 함수 호출 코드가 **함수 정의코드**로 치환

#### 인라인 함수의 장점
- 함수 호출에 따른 **오버헤드 제거**
- 실행 속도가 향상될 수 있음

#### 인라인 함수 선언 방법
- 일반함수 앞에 `inline` 키워드를 붙이면
    - **인라인 함수**로 선언
- `inline`키워드는 컴파일러에게 힌트일 뿐
- 컴파일러의 판단에 따라
    - 선언이 있어도 처리 X
    - 선언 없이 인라인 처리

#### 인라인 함수의 한계(단점)
- 함수 정의 방식이 일반 함수보다 **복잡**
- 함수 내용이 길거나 복잡하면
    - 인라인 처리되지 않거나
    - 코드 크기 증가로 비효율적
- **짧고 단순**한 함수에 적합

### 5. 매크로 함수
#### 매크로 함수란?
- 전처리 단계에서 **문자열 치환 방식**으로 처리되는 함수 형태의 매크로
- 함수 호출 개념이 없음
> #define SQUARE(x) ((x) * (x))

#### 매크로 함수의 장점
- 자료형에 독립적
    - 타입 개념이 없기 때문에
    - 모든 자료형에 동일하게 적용 가능
- 오버로딩이 필요없음
    - 인라인 함수는 자료형마다 오버로딩 필요
    - 매크로 함수는 하나의 정의로 모든 타입 처리 가능

#### 매크로 함수의 단점
- **타입 체크 불가**
    - 컴파일러가 아닌 전처리기 단계에서 처리
- 디버깅이 어려움
    - 오류 발생 시 실제 코드 위치 파악이 힘듦
- 부작용 발생 가능
    - 인자에 식이 들어갈 경우 예상치 못한 결과 발생
    > SQUARE(a++) → ((a++) * (a++))
- 코드 가독성과 유지보수성 저하

### 6. 템플릿(Template)
#### 템플릿이란?
- 대부분 인라인 함수로 동작
- 자료형에 독립적인 코드 작성
- 컴파일 시 자료형별 함수/클래스 생성
- 그 결과로 인라인 처리될 가능성이 높다

### 7. 매크로 함수와 인라인 함수(템플릿)
| 구분 | 매크로 함수 | 인라인 함수 | 템플릿 |
|---|---|---|---|
| 처리 시점 | 전처리 단계 | 컴파일 단계 | 컴파일 단계 |
| 처리 방식 | 문자열 치환 | 호출 위치에 함수정의 | 자료형별 함수 생성 |
| 자료형 | 자료형 독립적 | 자료형 종속적 | 자료형 독립적 |
| 오버로딩 | 불가능 | 가능 | 자동 생성 |
| 타입 검사 | 불가능 | 가능 | 가능 |
| 성능 | 빠름 | 빠를 수 있음 | 빠를 수 있음 |
| 특징 | 단순·위험 | 안전·힌트 | 정적 다형성 |

> 매크로 함수: 자료형에 독립적이지만 위험

> 인라인 함수: 안전하지만 자료형별 정의 필요

## 이름공간(namespace)

### 1. namespace의 기본 원리

#### namespace란?
- 이름 충돌(name collision)을 방지하기 위한 **이름 공간**
- 같은 이름의 함수, 변수라도 **다른 식별자로 취급**



중괄호 범위 지정 연산자

